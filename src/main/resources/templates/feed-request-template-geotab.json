{
  "method": "Get",
  "params": {
    "credentials": {
      "database": "{{dbName}}",
      "sessionId": "{{sessionId}}",
      "userName": "{{accessKey}}"
    },
    "typeName": "{{typeName}}"
  }
}

